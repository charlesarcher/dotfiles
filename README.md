# dotfiles (ARCHIVED)

This repository has been consolidated into **code-snips**:
https://github.com/charlesarcher/code-snips

- `scripts/*`  ->  `code-snips/scripts/`
- dotfiles      ->  `code-snips/dotfiles/` and `code-snips/system-scripts/`

Full git history preserved via subtree merge. This repo is retained only so
stray links can be found and squashed; do not add new content here.
Archived snapshot lives under `_archive/`.
